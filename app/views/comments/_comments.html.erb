<% todo.comments.each do |comment|  %>
  <div class="todo-comment">
    <%= comment.title %>
    <% if @owner %>
      <div  class="delete-btn">
      <%= link_to "Delete", todo_comment_path(todo, comment), data: {turbo_method: :delete } %>
    <% end%>
  </div>
<% end  %>
