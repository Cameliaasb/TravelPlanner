<%= simple_form_for [campaign, comment] do |f| %>
  <%=f.input :campaign_id, as: :hidden, input_html: { value: campaign.id } %>
  <%= f.input :title %>
  <%= f.button :submit, value: "New comment" %>
<% end %>
