<%  comments.each do |comment| %>
  <div class="comment">
    <%= comment.title %>
    <% if @owner %>
      <div class="delete-btn">
        <%= link_to "Delete", campaign_campaigncomment_path(campaign, comment), data: {turbo_method: :delete} %>
      </div>
    <% end%>
  </div>
<% end %>
